# ─────────────────────────────────────────────────────────────────
# Face Authorisation System – Python dependencies
# ─────────────────────────────────────────────────────────────────
#
# INSTALLATION NOTES
# ──────────────────
# 1. Install system libraries first (Ubuntu / Debian):
#      sudo apt-get install -y cmake build-essential libopenblas-dev \
#           liblapack-dev libx11-dev libgtk-3-dev python3-dev
#
# 2. (Recommended) Create and activate a virtual environment:
#      python -m venv .venv && source .venv/bin/activate
#
# 3. Install Python packages:
#      pip install -r requirements.txt
#
# NOTES ON dlib / face_recognition
# ─────────────────────────────────
# face_recognition depends on dlib which is compiled from source.
# On Windows, install CMake (https://cmake.org) and Visual Studio
# Build Tools before running pip install.
# On macOS: brew install cmake
#
# GPU acceleration (optional):
#   Use detection_model="cnn" in EngineConfig and set FACE_AUTH_MODEL=cnn.
#   Requires dlib compiled with CUDA support (see dlib docs).
# ─────────────────────────────────────────────────────────────────

# Core face recognition
face_recognition>=1.3.0
dlib>=19.24.0

# Image / video processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: for RTSP stream authentication helpers
# requests>=2.31.0
opencv-python
